<resources>

    <!-- global screen themes -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <!-- App toolbar theme -->
    <style name="ToolbarThemeLight" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorPrimary">@color/background_white</item>
        <item name="android:colorAccent">@color/background_white</item>
        <item name="android:colorPrimaryDark">@color/background_white</item>
    </style>

</resources>
